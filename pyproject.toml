[project]
name = "my-agent-config"
version = "0.1.0"
description = "Shared agent skills and lifecycle-aware installer"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.0",
]
